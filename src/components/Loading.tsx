import React from "react";

export default function Loading() {
  return (
    <div
      style={{
        height: "100dvh",
        alignItems: "center",
        justifyContent: "center",
        display: "flex",
        color: "black",
        background: "#E6FFEE",
        fontWeight: "bold",
      }}
    >
      Loading
    </div>
  );
}
