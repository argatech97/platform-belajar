export type RoleOption = { id: string; name: string };
export type SekolahOption = { id: string; name: string };
export type KelasOption = { id: number; tingkat: number };
export type Test = {
  id: string;
  name: string;
  jumlah_soal: number;
  durasi_seconds: number;
};
